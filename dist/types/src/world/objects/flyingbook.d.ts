import { WorldObject } from './worldobject';
export declare class FlyingBookObject extends WorldObject {
    private static image;
    private static smallestScale;
    private static smallestVelocity;
    constructor(spawnWidth: number, spawnHeight: number);
}
