import { WorldObject } from './worldobject';
export declare class BookStackObject extends WorldObject {
    private static images;
    constructor(spawnWidth: number, spawnHeight: number, xVelocity: number);
}
